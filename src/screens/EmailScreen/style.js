import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
      flex: 1,
    },
    mainContent: {
      flex: 1,
      margin: 10,
      marginTop: "10%",
      alignItems: "center",
    },
    textContainer: {
      marginVertical: "10%",
    },
    buttonContainer: {
      width: "70%",
    },
  });

  export default styles;